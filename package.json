{
  "name": "amaya-portfolio-site",
  "private": true,
  "version": "1.0.0",
  "description": "Build pipeline for Amaya's personal portfolio site. Run `npm run build` to regenerate the HTML pages from the content/ folder. GitHub Actions runs this automatically on every push.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
